<web-app>
    <!-- 其他配置 -->

    <!-- 配置ErrorController Servlet -->
    <servlet>
        <servlet-name>ErrorController</servlet-name>
        <servlet-class>com.example.webapp.ErrorController</servlet-class>
    </servlet>

    <!-- 配置ErrorController Servlet的处理URL -->
    <servlet-mapping>
        <servlet-name>ErrorController</servlet-name>
        <url-pattern>/error</url-pattern>
    </servlet-mapping>

    <!-- 其他配置 -->
</web-app>